import com.sun.prism.impl.Disposer;
import java.util.function.Function;

public class ReadCSV {


//    JavaSparkContext sc;
//    JavaRDD<String> data = sc.textFile("path/input.csv");
//    JavaSQLContext sqlContext = new JavaSQLContext(sc);
//
//    JavaRDD<Disposer.Record> rdd_records = sc.textFile(data).map(
//            new Function<String, Disposer.Record>() {
//                public Disposer.Record call(String line) throws Exception {
//                    // Here you can use JSON
//                    // Gson gson = new Gson();
//                    // gson.fromJson(line, Record.class);
//                    String[] fields = line.split(",");
//                    Disposer.Record sd = new Disposer.Record(fields[0], fields[1], fields[2].trim(), fields[3]);
//                    return sd;
//                }
//            });
}
